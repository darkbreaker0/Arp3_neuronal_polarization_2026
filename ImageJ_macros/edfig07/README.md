# `ImageJ_macros/edfig07/` — ED Fig 7 - ImageJ preprocessing macros

Fiji / ImageJ macros for **ED Fig 7 - ImageJ preprocessing macros**. Each macro preprocesses raw
imaging data into per-cell / per-neurite CSVs that the matching R Markdown
analysis in ``figures/`` consumes.

**3 macro file(s) in this folder.**

## Files

| File | Panel(s) | Role |
|------|----------|------|
| `edfig07_paraBlebb_roi_template.ijm` | (see header) | ROI intensity CSV generation. |
| `edfig07b_parablebb_gc_roi_batch.ijm` | ED Fig 7b | ROI intensity CSV generation; upstream of ED Fig 7b. |
| `edfig07d_parablebb_soma_roi_batch.ijm` | ED Fig 7d | ROI intensity CSV generation; upstream of ED Fig 7d. |

